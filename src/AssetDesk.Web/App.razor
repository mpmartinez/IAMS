@using AssetDesk.Web.Layout
@inject NavigationManager Navigation

<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                <NotAuthorized>
                    @if (context.User.Identity?.IsAuthenticated != true)
                    {
                        <RedirectToLogin />
                    }
                    else
                    {
                        <LayoutView Layout="@typeof(MainLayout)">
                            <p class="text-center py-8 text-muted-foreground">You are not authorized to view this page.</p>
                        </LayoutView>
                    }
                </NotAuthorized>
                <Authorizing>
                    @* Full-page loading - no layout shown during auth check *@
                    <div class="min-h-screen bg-muted flex items-center justify-center">
                        <div class="text-center">
                            <div class="w-12 h-12 border-4 border-primary border-t-transparent rounded-full animate-spin mx-auto mb-4"></div>
                            <p class="text-muted-foreground text-sm">@(IsLoggingOut ? "Logging out..." : "Checking authentication...")</p>
                        </div>
                    </div>
                </Authorizing>
            </AuthorizeRouteView>
            <FocusOnNavigate RouteData="@routeData" Selector="h1" />
        </Found>
        <NotFound>
            <PageTitle>Not Found</PageTitle>
            <LayoutView Layout="@typeof(MainLayout)">
                <div class="flex flex-col items-center justify-center py-16">
                    <div class="text-6xl font-bold text-muted-foreground mb-4">404</div>
                    <p class="text-muted-foreground mb-6">Page not found</p>
                    <a href="/" class="px-4 py-2 bg-primary text-white rounded-lg hover:bg-primary/90 transition-colors">
                        Go Home
                    </a>
                </div>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>

@code {
    private bool IsLoggingOut => Navigation.Uri.Contains("/authentication/logout", StringComparison.OrdinalIgnoreCase);
}
